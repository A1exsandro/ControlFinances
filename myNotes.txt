Mudando Js para Ts

# Criar o app usando expo init
# Criar um arquivo tsconfig.json && executar expo start && confirmar y
# Acrescentar "strict": true dentro de compilerOptions em tsconfig

Styled Components yarn add styled-components // yarn add @types/styled-components-react-native -D
    corrijir, pois instalei sem ser dependência de desenvolvimento
# Criando estilos globais